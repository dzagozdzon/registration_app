before_install:
  - psql -c 'create database registration_app_test' 

language: ruby

rvm: 
  - 2.6.3

script: 
  - bundle exec rails db:migrate RAILS_ENV=test
  - bundle exec rspec 

services: 
  - postgresql
